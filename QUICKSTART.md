# 🚀 Быстрый старт

## Запуск всей системы одной командой

### 1. Подготовка

Создайте файл `.env` в корне проекта:

```bash
cp .env.example .env
```

Заполните обязательные переменные в `.env`:

```env
OPENROUTER_API_KEY=your_key_here
API_BASE_URL=https://openrouter.ai/api/v1
MODEL_NAME=anthropic/claude-3.5-sonnet
```

### 2. Запуск

Запустите все сервисы:

```bash
docker-compose up -d
```

Это запустит:
- ✅ Redis (порт 66379)
- ✅ PostgreSQL (порт 55432)
- ✅ FastAPI Backend (порт 8000)
- ✅ Streamlit Frontend (порт 8501)

### 3. Проверка

Откройте в браузере:
- **Фронтенд**: http://localhost:8501
- **API Docs**: http://localhost:8000/docs
- **Health Check**: http://localhost:8000/health

### 4. Остановка

Остановить все сервисы:

```bash
docker-compose down
```

Остановить с удалением volumes (удалит все данные!):

```bash
docker-compose down -v
```

## Архитектура

```
┌─────────────────────────────────────────────────┐
│         Streamlit Frontend (8501)               │
│         http://localhost:8501                   │
└──────────────────┬──────────────────────────────┘
                   │ HTTP Requests
                   ▼
┌─────────────────────────────────────────────────┐
│         FastAPI Backend (8000)                  │
│  - REST API + WebSocket                         │
│  - Session Store (Redis)                        │
│  - Task Queue (asyncio)                         │
│  - Workers (2 router + 3 query)                 │
└──────────┬──────────────────┬───────────────────┘
           │                  │
           ▼                  ▼
┌──────────────────┐  ┌──────────────────┐
│  Redis (66379)   │  │ PostgreSQL       │
│  - Sessions      │  │ (55432)          │
│  - Cache         │  │ - Medical data   │
└──────────────────┘  └──────────────────┘
```

## Логи

Смотреть логи всех сервисов:

```bash
docker-compose logs -f
```

Смотреть логи конкретного сервиса:

```bash
docker-compose logs -f fastapi
docker-compose logs -f streamlit
docker-compose logs -f redis
docker-compose logs -f postgres
```

## Перезапуск отдельных сервисов

Перезапустить FastAPI (после изменения кода):

```bash
docker-compose restart fastapi
```

Перезапустить Streamlit:

```bash
docker-compose restart streamlit
```

## Разработка

Для разработки можно запускать сервисы локально:

### Только инфраструктура:

```bash
docker-compose up -d redis postgres
```

### FastAPI локально:

```bash
cd fastapi_app
pip install -r requirements.txt
python main.py
```

### Streamlit локально:

```bash
cd streamlit_app
pip install -r requirements.txt
streamlit run app.py
```

## Проблемы и решения

### Порты заняты

Если порты 6379 или 5432 уже заняты, они были изменены на:
- Redis: 66379
- PostgreSQL: 55432

### FastAPI не стартует

Проверьте:
1. Redis доступен: `docker-compose logs redis`
2. PostgreSQL доступен: `docker-compose logs postgres`
3. .env файл заполнен правильно

### Streamlit не может подключиться к API

Проверьте:
```bash
curl http://localhost:8000/health
```

Должен вернуть:
```json
{
  "status": "healthy",
  "service": "Medical Analytics Agent v2",
  "router_queue_size": 0,
  "query_queue_size": 0
}
```

## Мониторинг

### Проверка состояния сервисов:

```bash
docker-compose ps
```

### Проверка очередей:

```bash
curl http://localhost:8000/health
```

### Redis CLI:

```bash
docker exec -it ai-med-brat-redis redis-cli
> KEYS session:*
> GET session:<session-id>
```

### PostgreSQL:

```bash
docker exec -it ai-med-brat-postgres psql -U postgres -d medical_db
\dt  -- Список таблиц
```

## Полезные команды

Пересобрать образы после изменения кода:

```bash
docker-compose up -d --build
```

Очистить всё и начать с нуля:

```bash
docker-compose down -v
docker system prune -a
docker-compose up -d --build
```

## Производительность

### Масштабирование воркеров

Откройте [fastapi_app/main.py](fastapi_app/main.py:64-69) и измените:

```python
await task_queue.start_workers(
    router_handler=router_worker.process_task,
    query_handler=query_worker.process_task,
    num_router_workers=5,  # Увеличьте для большей пропускной способности
    num_query_workers=10,  # Увеличьте для параллельной обработки
)
```

### Горизонтальное масштабирование

Запустите несколько инстансов FastAPI:

```bash
docker-compose up -d --scale fastapi=3
```

Используйте nginx как load balancer (см. README_v2.md)

## Что дальше?

Прочитайте полную документацию:
- [README_v2.md](README_v2.md) - Полное описание архитектуры
- [API документация](http://localhost:8000/docs) - Swagger UI