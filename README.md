# 🏥 MEDBRAT.AI

MEDBRAT.AI это мультиагентная система, которая анализирует медицинские данные о пациентах, диагнозах, рецептах и медикаментах в Санкт-Петербурге и Ленинградской области. Агент автоматически формирует SQL-запросы и строит визуализации.

## 🚀 Как запустить

### Предварительные требования

- Docker и Docker Compose

### Быстрый старт

1. **Клонируйте репозиторий**
```bash
git clone https://github.com/20twomay/ai-med-brat.git
cd ai-med-brat
```

2. **Создайте файл `.env`**
```bash
cp .env.example .env
```

Отредактируйте `.env` и укажите:
```env
# OpenRouter API
OPENROUTER_API_KEY=your_api_key
API_BASE_URL=https://openrouter.ai/api/v1
MODEL_NAME=anthropic/claude-3.5-sonnet

# LangSmith (optional - для трейсинга)
LANGSMITH_TRACING=true
LANGSMITH_API_KEY=your_langsmith_api_key_here
LANGSMITH_PROJECT=your_project_name_here
LANGSMITH_ENDPOINT=https://api.smith.langchain.com
```

3. **Запустите сервисы**
```bash
docker compose up -d --build
```

4. **Проверьте работу**
- Streamlit (если запущен): http://localhost:8501


```
.
├── fastapi_app/          # FastAPI приложение
│   ├── agent/            # Логика AI-агента
│   │   ├── agents.py     # Основной агент
│   │   ├── tools.py      # SQL, визуализация, ML
│   │   ├── prompts.py    # Промпты для LLM
│   │   └── models.py     # Pydantic модели
│   ├── main.py           # Точка входа API
│   └── requirements.txt
├── streamlit_app/        # Streamlit UI (опционально)
├── data/                 # База данных PostgreSQL
│   ├── processed/        # CSV с данными
│   └── postgres_db/      # Скрипты инициализации
├── docker-compose.yml
└── README.md
```

## 🗃️ База данных

### Схема

Система работает с 4 основными таблицами:

- **patients**: пациенты (дата рождения, пол, район, регион)
- **diagnoses**: диагнозы (код МКБ-10, название, класс)
- **medication**: препараты (код, дозировка, название, стоимость)
- **recipes**: рецепты (дата, связи с пациентом/диагнозом/препаратом)

## 🛠️ Технологии

- **Backend**: FastAPI, LangChain, LangGraph, SQLAlchemy
- **LLM**: Qwen 3 Coder 30B A3B Instruct (через OpenRouter)
- **Viz**: Matplotlib, Seaborn
- **DB**: PostgreSQL 16
- **Storage**: MinIO (S3-совместимое хранилище)
