# Streamlit Frontend для MedBrat.AI

## 📋 Описание

Frontend приложение на Streamlit для медицинского ассистента MedBrat.AI. Предоставляет пользовательский интерфейс для взаимодействия с аналитической системой медицинских данных.

## 🏗️ Архитектура

### Структура проекта

```
streamlit_app/
├── app.py                      # Главная страница - маршрутизация
├── config.py                   # Централизованная конфигурация
├── constants.py                # Константы приложения
├── styles.py                   # Централизованные стили
├── api_client.py              # API клиент для взаимодействия с backend
├── components.py              # Переиспользуемые UI компоненты
├── requirements.txt           # Зависимости Python
├── Dockerfile                 # Docker-образ для контейнеризации
├── core/                      # Основные модули
│   ├── __init__.py
│   ├── auth.py               # Утилиты аутентификации
│   ├── session.py            # Управление сессиями Streamlit
│   └── storage.py            # Работа с LocalStorage
├── pages/                    # Страницы приложения
│   ├── 1_auth.py            # Страница авторизации
│   └── 2_chat.py            # Страница чата
└── src/                     # Статические ресурсы
    └── logo.svg            # Логотип приложения
```

### Основные компоненты

#### 1. **config.py** - Централизованная конфигурация
- `AppConfig` - основная конфигурация приложения (API URL, таймауты, лимиты)
- `PageConfig` - конфигурация страниц Streamlit
- `PAGE_CONFIGS` - словарь конфигураций для каждой страницы

#### 2. **constants.py** - Константы
- HTTP статус-коды
- Ключи session state
- Ключи LocalStorage
- Валидация паролей
- Конфигурация retry
- UI сообщения
- Роли и типы артефактов

#### 3. **styles.py** - Стили
- Цветовая палитра
- CSS стили для sidebar
- CSS стили для формы чата
- Генераторы HTML для UI компонентов

#### 4. **core/** - Основные модули

##### core/auth.py
- `validate_password_length()` - валидация пароля
- `check_token_from_localstorage()` - auto-login из LocalStorage
- `logout()` - выход из системы
- `require_authentication()` - проверка авторизации (middleware)
- `get_api_client()` - получение настроенного API клиента

##### core/session.py
- `init_session_state()` - инициализация session state
- `check_authentication()` - проверка статуса авторизации
- `clear_session_state()` - очистка сессии

##### core/storage.py
- `get_token_from_localstorage()` - получение токена из браузера
- `save_token_to_localstorage()` - сохранение токена
- `remove_token_from_localstorage()` - удаление токена

#### 5. **api_client.py** - API клиент
Централизованный клиент для всех запросов к FastAPI backend:
- Аутентификация: `register()`, `login()`, `get_user_info()`
- Чаты: `get_chats()`, `create_chat()`, `delete_chat()`
- Сообщения: `get_chat_messages()`
- Выполнение запросов: `execute_query()`, `execute_query_with_retry()`
- Health check: `get_health()`

Особенности:
- Автоматическое добавление Authorization header
- Обработка ошибок и логирование
- Retry механизм для долгих запросов
- Типизация через type hints

#### 6. **components.py** - UI компоненты
Переиспользуемые компоненты интерфейса:
- `render_logo()` - логотип приложения
- `render_chat_list()` - список чатов в sidebar
- `render_user_profile_button()` - кнопка профиля пользователя
- `render_context_indicator()` - индикатор использования контекста
- `render_logout_button()` - кнопка выхода

### Потоки данных

#### Аутентификация
```
1. Пользователь → 1_auth.py → APIClient.login() → FastAPI backend
2. Backend → access_token + user_info
3. Streamlit → session_state + LocalStorage
4. Auto-redirect → 2_chat.py
```

#### Работа с чатами
```
1. Загрузка чатов: APIClient.get_chats() → sidebar
2. Создание чата: Кнопка → APIClient.create_chat() → новый chat_id
3. Переключение: Клик на чат → загрузка истории → APIClient.get_chat_messages()
4. Удаление: Кнопка ⨯ → APIClient.delete_chat()
```

#### Обработка запросов
```
1. Ввод сообщения → добавление в session_state.messages
2. st.rerun() → отображение сообщения пользователя
3. APIClient.execute_query_with_retry() → обработка на backend
4. Получение ответа + артефакты (charts, tables)
5. Добавление в session_state.messages
6. st.rerun() → отображение ответа ассистента
```

### Session State

Ключевые переменные сессии:
```python
{
    "authenticated": bool,           # Статус авторизации
    "token": str,                   # JWT токен
    "user_info": dict,              # Информация о пользователе
    "chat_id": int,                 # ID текущего чата
    "messages": list,               # История сообщений
    "total_tokens": int,            # Использовано токенов в чате
    "messages_loaded": bool,        # Флаг загрузки истории
    "show_profile_modal": bool,     # Отображение профиля
}
```

### Обработка артефактов

Система поддерживает два типа артефактов:

#### Charts (Plotly графики)
```python
{
    "type": "chart",
    "url": "{API_URL}/charts/{chat_id}/{filename}.json"
}
```
- Загружается как JSON
- Отображается через `st.plotly_chart()`

#### Tables (CSV файлы)
```python
{
    "type": "csv",
    "url": "{API_URL}/charts/{chat_id}/{filename}.csv"
}
```
- Загружается как CSV
- Отображается информация + кнопка скачивания

## 🚀 Запуск

### Локальный запуск

```bash
# Установка зависимостей
pip install -r requirements.txt

# Запуск приложения
streamlit run app.py
```

### Docker

```bash
# Сборка образа
docker build -t medbrat-streamlit .

# Запуск контейнера
docker run -p 8501:8501 \
  -e API_URL=http://localhost:8000 \
  medbrat-streamlit
```

### Docker Compose

```bash
# Запуск всего стека (из корня проекта)
docker compose up -d
```

## ⚙️ Конфигурация

### Переменные окружения

```bash
# URL FastAPI backend
API_URL=http://localhost:8000

# Уровень логирования
LOG_LEVEL=INFO
```

### Настройки в config.py

```python
@dataclass
class AppConfig:
    api_url: str = os.getenv("API_URL", "http://localhost:8000")
    api_timeout: int = 60
    context_limit: int = 256000
    max_retries: int = 3
    default_chats_limit: int = 100
    default_messages_limit: int = 100
    min_password_length: int = 6
    max_password_length_bytes: int = 72
```

## 🎨 Кастомизация UI

### Цветовая схема (styles.py)
```python
PRIMARY_COLOR = "#16A8B1"           # Основной цвет
PRIMARY_GRADIENT = "linear-gradient(135deg, #16A8B1 0%, #2EA9AC 100%)"
```

### Модификация компонентов
Все UI компоненты находятся в `components.py` и могут быть легко изменены.

## 🔒 Безопасность

### Аутентификация
- JWT токены для авторизации
- Автоматическое сохранение в LocalStorage
- Auto-login при перезагрузке страницы
- Безопасная очистка токенов при logout

### Валидация
- Валидация паролей (длина, сложность)
- Проверка входных данных перед отправкой
- Обработка ошибок API

## 📊 Логирование

Логи выводятся в формате:
```
[STREAMLIT] 2024-12-13 10:30:45 - User logged in: user@example.com
[STREAMLIT] 2024-12-13 10:31:02 - Loading message history for chat_id=123
```

Уровень логирования настраивается через `LOG_LEVEL`.

## 🧪 Тестирование

```bash
# Запуск тестов (если есть)
pytest tests/

# Проверка типов
mypy streamlit_app/
```

## 📦 Зависимости

Основные зависимости:
- `streamlit` - фреймворк для web-приложений
- `requests` - HTTP клиент
- `pandas` - работа с данными
- `plotly` - интерактивные графики

Полный список в [requirements.txt](requirements.txt)

## 🐛 Troubleshooting

### Проблемы с авторизацией
1. Проверьте доступность backend (`/health` endpoint)
2. Очистите LocalStorage в браузере
3. Проверьте переменную окружения `API_URL`

### Графики не отображаются
1. Проверьте Authorization header
2. Убедитесь что путь к файлу корректный
3. Проверьте логи backend на наличие ошибок

### Проблемы с LocalStorage
1. Проверьте что JavaScript включен в браузере
2. Попробуйте очистить кэш браузера
3. Проверьте консоль браузера на ошибки

## 📝 Дальнейшее развитие

- [ ] Поддержка markdown в сообщениях
- [ ] Возможность редактирования сообщений
- [ ] Экспорт истории чата
- [ ] Настройки темы (светлая/темная)
- [ ] Поддержка voice input
- [ ] Мультиязычность (i18n)

## 📄 Лицензия

MIT License

## 👥 Авторы

MedBrat.AI Team
